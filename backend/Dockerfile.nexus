# Stage 1: Build the Spring Boot app using Maven
FROM maven:3.8.5-openjdk-17 AS build

WORKDIR /app

<<<<<<< HEAD
# Copy the entire backend directory content into the build container
COPY . .

# Run Maven build (skip tests)
RUN ./mvnw clean install -DskipTests

# Stage 2: Use a lightweight JDK base image for running the app
FROM openjdk:17-jdk-slim

# Set work directory
WORKDIR /app

# Copy the built JAR file from the previous stage
COPY --from=build /app/target/*.jar app.jar

# Expose port (adjust if different)
EXPOSE 8080

# Run the app
=======
# Copy the entire backend project into the container
COPY . .

# 🛠 Explicitly give execution rights to mvnw
RUN chmod +x mvnw && ./mvnw clean install -DskipTests

# Stage 2: Run with a lightweight JDK
FROM openjdk:17-jdk-slim

WORKDIR /app

COPY --from=build /app/target/*.jar app.jar

EXPOSE 8080

>>>>>>> 9a92bce (✅ Fix Dockerfile by adding chmod for mvnw)
ENTRYPOINT ["java", "-jar", "app.jar"]
